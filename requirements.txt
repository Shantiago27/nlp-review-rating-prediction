# Tested against Python 3.11 (the Google Colab runtime this notebook was
# originally run on, per the paths in the saved outputs).
#
# num2words==0.5.14 is pinned exact because it's the confirmed version from
# the notebook's own saved `pip install` output (docopt is one of its own
# transitive dependencies, not listed here directly).
# Everything else is a minimum-version floor rather than a verified pin —
# the actual Colab versions were never confirmed.
#
# gensim was removed (it was only used for an accessory word-embeddings
# visualization, not part of the actual TF-IDF + LogisticRegression /
# RandomForest pipeline), so there's no numpy<2 constraint here either —
# that cap only existed for gensim's sake.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
nltk>=3.8
wordcloud>=1.9
matplotlib>=3.7
num2words==0.5.14
